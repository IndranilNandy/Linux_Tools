#!/bin/bash

# Insert entry of this tool in .allTools. This is to be used in the verification stage post setup.
grep -q "androidstudio:" ../../_verify/.allTools || echo "androidstudio:androidstudio" >>../../_verify/.allTools

#Check if all prerequisites are installed, and install the missing ones
prereq --install ".prereq"

# Exit if still missing any prerequisite
[[ $(prereq --missing ".prereq") ]] && echo -e "${RED}One or more prerequisites are still missing. Exiting.${RESET}" && exit 0

# Check if it is already installed
[[ -n $(which androidstudio) ]] && echo -e "\n${GREEN}[androidstudio] Already installed. Exiting.${RESET}" && exit 0

echo -e "${YELLOW}[androidstudio] Setup process started.${RESET}"
./androidstudio_installer_from_binary.sh
echo -e "${GREEN}[androidstudio] Setup finished.${RESET}"
