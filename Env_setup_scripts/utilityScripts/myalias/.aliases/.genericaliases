# Aliases for GENERIC commands

c=clear
sctl=systemctl
lsa=ls -alut

p+x=find . -name "*.sh" -exec chmod +x {} \;
p-x=find . -name "*.sh" -exec chmod -x {} \;
p=find . -name "*.sh" -exec ls -alut {} \;

ccom=cd /usr/local/bin/mycommands
cref= cd /usr/local/bin/myscriptrefs
ctool=cd $HOME/MyTools/Linux_Tools

s=shutdown now
